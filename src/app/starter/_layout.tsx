import AppTabs from '@/components/app-tabs';

/**
 * The Expo template screens, kept intact and reachable at /starter while the
 * ELSEA product screens are built out. Not customer-facing.
 */
export default function StarterLayout() {
  return <AppTabs />;
}
